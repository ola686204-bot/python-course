"""Entry point for the modular persistent contact manager."""

import contacts
import display
import file_handler
import session
import validator


if __name__ == "__main__":
    session.run_session()
